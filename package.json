{
  "name": "studioos",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "echo \"Run dev scripts inside apps\"",
    "install-hooks": "git config core.hooksPath .githooks && chmod +x .githooks/pre-commit && echo 'Git hooks installed from .githooks'"
  },
  "dependencies": {
    "rxjs": "^7.8.2",
    "uid": "^2.0.2"
  }
}
